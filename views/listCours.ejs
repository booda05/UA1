<!-- views/listCours.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Liste des cours</title>
    <link rel="stylesheet" href="/css/listStyle.css">
</head>
<body>
    <h1>Liste des cours</h1>
    <div class="buttons-container">
        <a href="/" class="button">Accueil</a>
        <a href="/cours/add" class="button">Ajouter un Cours</a>
    </div>    
    <form action="/cours/search" method="get">
       
        <input type="text" name="query" placeholder="Rechercher un cours" value="<%= searchQuery %>">
        <button type="submit">Rechercher</button>
    </form>
    <% var query = query || ''; %>
    <% if (query) { %>
        <div class="buttons-container">
            <a href="/cours" class="button">Retour</a>
        </div>
    <% } %>
   
     <% var searchQuery = typeof query !== 'undefined' ? query : ''; %>
    <% if (!found) { %>
        <p>Cours introuvable.</p>
    
    <% } else { %>
        <table>
            <thead>
                <tr>
                    <th>Titre</th>
                    <th>Description</th>
                </tr>
            </thead>
            <tbody>
               
                <% cours.forEach(function(course) { %>
                    
                    <tr>
                        <td><%= course.title %></td>
                        <td><%= course.description %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        
    <% } %>
</body>
</html>
